--!strict
-- Client bootstrap: combat input + all UI modules.

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Shared = ReplicatedStorage:WaitForChild("Shared")
local Remotes = require(Shared.Remotes)

local CombatController = require(script.Controllers.CombatController)
local HUD = require(script.UI.HUD)
local RoleCard = require(script.UI.RoleCard)
local RevealScreen = require(script.UI.RevealScreen)
local MapVote = require(script.UI.MapVote)
local Shop = require(script.UI.Shop)
local Pings = require(script.UI.Pings)
local Touch = require(script.UI.Touch)
local Effects = require(script.UI.Effects)
local Juice = require(script.UI.Juice)
local Daily = require(script.UI.Daily)
local Quests = require(script.UI.Quests)
local Modifiers = require(script.UI.Modifiers)
local Leaderboard = require(script.UI.Leaderboard)
local Season = require(script.UI.Season)
local Ability = require(script.UI.Ability)
local Social = require(script.UI.Social)
local Nametags = require(script.UI.Nametags)
local Tutorial = require(script.UI.Tutorial)
local Settings = require(script.UI.Settings)
local Sounds = require(script.UI.Sounds)

local remotes = Remotes.client()

local actions = CombatController.start(remotes)
HUD.start(remotes)
RoleCard.start(remotes)
RevealScreen.start(remotes)
MapVote.start(remotes)
Shop.start(remotes)
Pings.start(remotes)
Touch.start(remotes, actions)
Effects.start(remotes)
Juice.start(remotes)
Daily.start(remotes)
Quests.start(remotes)
Modifiers.start(remotes)
Leaderboard.start(remotes)
Season.start(remotes)
Ability.start(remotes)
Social.start(remotes)
Nametags.start(remotes)
Tutorial.start(remotes)
Settings.start()
Sounds.start(remotes)

print("[INFECTED] Client booted")
